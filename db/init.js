const db = require('./database');

// This file can be used to run migrations or seed initial data
// Currently, tables are created automatically in database.js

console.log('Database initialized successfully');

module.exports = db;
