services:
  lifeos:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - lifeos-data:/data
      - ./config:/app/config
    restart: unless-stopped
    environment:
      - PORT=3000
      - DATABASE_PATH=/data/lifeos.db

volumes:
  lifeos-data:
